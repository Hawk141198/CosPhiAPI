{
  "name": "CosPhi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm-run-all  --parallel watch:**",
    "watch:src": "parcel watch src/*.js -d . --target node",
    "build": "npm-run-all  --parallel build:**",
    "build:src": "parcel build src/*.js -d . --target node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.28.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.23.4",
    "nodemon": "^2.0.6"
  }
}
